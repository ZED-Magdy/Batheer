@model IEnumerable<BaSaleh.Application.TheIntendedBeneficiaries.Queries.GetTheIntendedBeneficiaries.TheIntendedBeneficiariesOfTheScheduledAssociationProject>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TargetedSchedulingForProjectImplementationId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BaseRequestId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsDeleted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsDeletedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsDeletedBy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TargetedSchedulingForProjectImplementationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BaseRequestId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeleted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeletedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeletedBy)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
